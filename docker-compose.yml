version: '2'

services:

  node-app:
    build: .
    image: node-app:1.0
    ports:
      - '9999:9999'

  PostgreSQL:

    image: postgres:12-alpine
    ports:
      - '3306:3306'
    environment:
      - POSTGRES_DB=app
      - POSTGRES_USER=app
      - POSTGRES_PASSWORD=pass